name: C++ Build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Compile
        run: g++ main.cpp -o main

      - name: Run Program
        run: ./main
